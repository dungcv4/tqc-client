// Source: dump.cs TypeDefIndex 902 — `enum Direction` in namespace AnimationOrTween

namespace AnimationOrTween
{
    public enum Direction
    {
        Reverse = -1,
        Toggle = 0,
        Forward = 1,
    }
}
